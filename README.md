## Setup

To run the backend:

* Ensure a PostgreSQL server is running on port `5432`
* Create a database named `lutetiawebshop`

This is required so the backend seeder can populate mock data correctly.
